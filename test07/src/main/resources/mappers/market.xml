<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tsherpa.team35.per.MarketMapper">
    <insert id="marketInsert" >
        insert into market(login_id, title, content,price,conditions)
        values(#{loginId}, #{title}, #{content}, #{price}, #{conditions})
        <selectKey resultType="int" keyProperty="market_no" order="AFTER">
            select LAST_INSERT_ID()
        </selectKey>
    </insert>
</mapper>